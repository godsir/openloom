{
  "name": "openloom",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "node scripts/kill-stale.js",
    "dev": "concurrently -n server,electron -c blue,green \"cargo run --bin loom-server -- serve --port 19876\" \"cd electron && node launch.cjs --dev\"",
    "dev:server": "cargo run --bin loom-server -- serve --port 19876",
    "dev:electron": "cd electron && node launch.cjs --dev",
    "build": "cargo build --release && cd web && npm run build",
    "build:electron": "cd electron && npm run build:win"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
