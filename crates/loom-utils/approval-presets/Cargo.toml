[package]
name = "loom-approval-presets"
version = "0.2.0"
edition = "2024"
license = "Apache-2.0"

[lints]
workspace = true

[dependencies]
loom-protocol = { workspace = true }

[lib]
test = false
doctest = false
