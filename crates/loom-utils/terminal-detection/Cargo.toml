[package]
name = "loom-terminal-detection"
version = "0.2.0"
edition = "2024"
license = "Apache-2.0"

[lib]
name = "loom_terminal_detection"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true

[dependencies]
tracing = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }
