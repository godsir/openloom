[package]
name = "openloom-cache"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
safetensors = "0.4"

[dev-dependencies]
serde_json = "1"
