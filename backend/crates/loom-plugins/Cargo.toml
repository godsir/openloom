[package]
name = "loom-plugins"
version = "0.3.22"
edition = "2024"
description = "Plugin discovery and manifest loading for openLoom v2 �?Claude Code / OpenClaw compatible"

[dependencies]
anyhow = "1"
tracing = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9"
